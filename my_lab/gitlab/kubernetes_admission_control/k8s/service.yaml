apiVersion: v1
kind: Service
metadata:
  name: clean-signed-service
  namespace: pep-test
spec:
  type: NodePort
  selector:
    app: clean-signed-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30221