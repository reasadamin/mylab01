apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-kusc00401
  name: nginx-kusc00401
spec:
  containers:
    - image: nginx
      name: nginx
  # Exam-intended approach: match a node label with nodeSelector
  # nodeSelector:
  #   disktype: ssd
  #   # or
  #   disktype: spinning
  # Demonstrated in the lab by pinning directly with nodeName:
  nodeName: minikube-m02
